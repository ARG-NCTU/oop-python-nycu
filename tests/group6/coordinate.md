# add mermaid cladd diagram for class coordinate
```mermaid
classDiagram
    class coordinate{
        -x:double
        -y:double
        +__init__(x,y):void
        +__str__():str
        +distance(other):double
    }
```
